name: Test

on:
  push:
  pull_request:
  schedule:
    - cron: '0 15 * * 5'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: ruby/setup-ruby@v1
      with:
        ruby-version: '3.0'
        bundler-cache: true
    - name: Install
      run: |
        curl -LOfsS https://github.com/Palakis/obs-websocket/releases/download/4.9.0/obs-websocket_4.9.0-1_amd64.deb
        sha256sum <<<"52d0dd7b89fc48db74986f6a8824c19229b5c8563e8937907894a493c4dbf56f  ./obs-websocket_4.9.0-1_amd64.deb"

        sudo add-apt-repository ppa:obsproject/obs-studio
        sudo apt-get update -qq
        sudo apt-get install -y --no-install-recommends ./obs-websocket_4.9.0-1_amd64.deb xvfb
    - name: Start OBS
      run: spec/run_obs &
    - name: Test
      run: bundle exec rake
    - name: Run examples
      working-directory: examples/websocket-driver
      run: |
        bundle install
        ./main.rb ws://localhost:4444 password

  codegen:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: ruby/setup-ruby@v1
      with:
        ruby-version: '3.0'
        bundler-cache: true
    - name: Generate code
      run: bundle exec rake codegen
    - name: Check diff
      run: |
        if ! git diff --quiet; then
          echo "ERROR: Generated code is outdated" >&2
          exit 1
        fi
